{
  "name": "@sya/function-scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "SYA scheduler function runtime.",
  "scripts": {
    "build": "scripts/package-runtime.sh current",
    "test": "scripts/smoke-test.sh"
  }
}
